---
# ══════════════════════════════════════════════════════════════════════════════
# LOCAL ENVIRONMENT FILE — copy to env.yml and fill in before running
# cp group_vars/all/env.yml.template group_vars/all/env.yml
# ══════════════════════════════════════════════════════════════════════════════

# GitHub PAT with read access to homelab-nebulahvelvet
# Generate at: GitHub → Settings → Developer settings → Fine-grained tokens
flux_github_token: ""

# SSH public keys to add to all nodes
# Run: cat ~/.ssh/id_ed25519.pub
ssh_authorized_keys:
  - ""